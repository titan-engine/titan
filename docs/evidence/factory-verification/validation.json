{
  "source_revision": "e4800939606889669e8a9b04650cda4bce6df37d",
  "date_utc": "2026-09-06",
  "working_changes": "Verification documents, fixtures and disposable probes only; game source unchanged",
  "passed_commands": [
    "cargo fmt --all --check",
    "cargo test --workspace --all-targets",
    "cargo clippy --workspace --all-targets --all-features -- -D warnings",
    "cargo check -p titan -p titan-protocol -p titan-browser --target wasm32-unknown-unknown",
    "cargo fmt --manifest-path games/factory/Cargo.toml --all --check",
    "cargo test --manifest-path games/factory/Cargo.toml --all-targets",
    "cargo clippy --manifest-path games/factory/Cargo.toml --all-targets --all-features -- -D warnings",
    "python3 games/factory/scripts/test-control.py",
    "python3 games/factory/scripts/build-browser.py",
    "node games/factory/scripts/test-browser.mjs",
    "node --test games/factory/web/inspector/*.test.mjs games/factory/web/play/*.test.mjs",
    "cargo test --manifest-path games/factory/Cargo.toml --test render -- --ignored",
    "python3 docs/evidence/factory-verification/player/verify-traces.py"
  ],
  "native_gpu_tests": {"passed": 2, "failed": 0},
  "note": "Exact PR-head remote CI and independent review are linked in the PR; GUI observations are recorded separately, not inferred from Node or golden tests."
}
