diff --git a/games/factory/src/game.rs b/games/factory/src/game.rs
index 86a0be0..a2514ca 100644
--- a/games/factory/src/game.rs
+++ b/games/factory/src/game.rs
@@ -568,7 +568,7 @@ pub fn inspector_with_capture(
         )
         .unwrap();
     inspector
-        .register_read_only_field::<Structure, _>("remaining", field("u32", "Processor work ticks, 0..120; zero with an in-process ore is blocked finished work"), |s| s.remaining)
+        .register_read_only_field::<Structure, _>("remaining", field("u32", "Processor work ticks, 0..90; zero with an in-process ore is blocked finished work"), |s| s.remaining)
         .unwrap();
     inspector
         .register_query(
diff --git a/games/factory/src/game/interface.rs b/games/factory/src/game/interface.rs
index 6148b7e..a762a20 100644
--- a/games/factory/src/game/interface.rs
+++ b/games/factory/src/game/interface.rs
@@ -185,7 +185,7 @@ pub(super) fn enrich(world: &World, s: &Structure, value: &mut Value) {
         (
             "working",
             "Working",
-            "Converting one ore into one plate in 120 work ticks.",
+            "Converting one ore into one plate in 90 work ticks.",
             "Follow the work progress and output connection.",
         )
     } else if s.slots.output.is_some() && !matches!(route["code"].as_str(), Some("ready")) {
@@ -213,7 +213,7 @@ pub(super) fn enrich(world: &World, s: &Structure, value: &mut Value) {
         (
             "working",
             "Ready to process",
-            "One ore is queued for a new 120-tick batch.",
+            "One ore is queued for a new 90-tick batch.",
             "Advance a tick or resume.",
         )
     } else if s.kind == Kind::Processor {
@@ -263,7 +263,7 @@ pub(super) fn enrich(world: &World, s: &Structure, value: &mut Value) {
             json!({"label":"Deposit to 1 ore / 60 eligible ticks","elapsed":s.progress,"total":60})
         }
         Kind::Processor => {
-            json!({"label":"1 ore to 1 plate / 120 work ticks","elapsed":if s.slots.in_process.is_some(){120-s.remaining}else{0},"total":120})
+            json!({"label":"1 ore to 1 plate / 90 work ticks","elapsed":if s.slots.in_process.is_some(){90-s.remaining}else{0},"total":90})
         }
         _ => Value::Null,
     };
diff --git a/games/factory/src/game/production.rs b/games/factory/src/game/production.rs
index 07c8657..efe2a1d 100644
--- a/games/factory/src/game/production.rs
+++ b/games/factory/src/game/production.rs
@@ -26,7 +26,7 @@ pub(super) fn produce(s: &mut Structure, extracted: &mut u64) -> Result<(), Stri
             if s.slots.in_process.is_none() && s.slots.input == Some(Item::Ore) {
                 s.slots.input = None;
                 s.slots.in_process = Some(Item::Ore);
-                s.remaining = 120;
+                s.remaining = 90;
             }
         }
         _ => {}
